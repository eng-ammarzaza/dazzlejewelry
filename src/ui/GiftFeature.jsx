function GiftFeature({ title, description }) {
  return (
    <div className="text-center">
      <p className="font-giftingTitle text-xl my-2">{title}</p>
      <p className=" font-giftingDesc text-xl mx-4">{description}</p>
    </div>
  );
}

export default GiftFeature;
